---
title: "Changelog"
description: "How changelog entries are maintained."
---

Keep meaningful user-facing changes in `CHANGELOG.md` under `Unreleased`.

Use:

```bash
scripts/check-changelog.sh
```

Docs structure changes, public command changes, REST API changes, and MCP tool changes should be recorded because they affect developer workflows.

